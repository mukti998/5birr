name: birr5
description: 5BIRR — location-based multi-sector marketplace and service platform.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.6
  google_maps_flutter: ^2.7.0
  geolocator: ^12.0.0
  firebase_core: ^3.1.0
  firebase_messaging: ^15.0.0
  go_router: ^14.2.0
  flutter_riverpod: ^2.5.1
  cached_network_image: ^3.3.1
  intl: ^0.19.0
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
